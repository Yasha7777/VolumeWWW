# PROGRESS

<!-- Каждая значимая правка — новой строкой: что изменил и в каком файле.
     Всегда начинай строку с даты в формате [ГГГГ-ММ-ДД]. -->

[2026-08-09] CubesHeroImpl.jsx — DodecahedronGeometry detail 0→1 (~144 граней), IcosahedronGeometry detail 0→2 (~320 граней); добавлен случайный несимметричный масштаб по Y/Z (0.72–1.28) для органичного вида камней без потери производительности (2 InstancedMesh draw-call не меняются).
[2026-08-09] History.jsx — кнопка «Скачать все» теперь собирает ZIP-архив через fflate (store-mode, параллельная загрузка) вместо поочерёдных blob-скачиваний; добавлено состояние «Архивирую…» во время формирования архива.
[2026-08-09] swag.css — флора (медведи) теперь видна на тёмной теме: убрано `display:none`, добавлен `filter:invert(1)` + opacity 0.32 вместо 0.72 (светлые линии на тёмном фоне визуально ярче, чем тёмные на кремовом).

- Hero переведён на scroll-scrubbed сборку кучи (прогресс = scrollY секции, без setTimeout): `frontend/src/components/three/CubesHeroImpl.jsx` — переписан на две текстурированные InstancedMesh (гравий-додекаэдры + песок-икосаэдры), сборка/рассыпание строго по позиции скролла, prefers-reduced-motion → статичная собранная куча.
- Сгенерированы и добавлены текстуры материала (higgsfield nano_banana): `frontend/public/textures/gravel.png`, `frontend/public/textures/sand.png` (карельская палитра: камень/охра/лес).
- Канвас героя сделан fixed во вьюпорте (sticky на время эффекта, гаснет после сборки): `frontend/src/styles.css` (.hero .kb-hero3d → position: fixed).
- Скролл резче: Lenis duration 1.05→0.6, добавлены lerp 0.12 и cubic-out easing: `frontend/src/components/SmoothScroll.jsx`.
- Удалён `frontend/public/models/cube_and_cave.glb` (40 МБ, раздувал деплой).
- Рельеф hero: normal-карта выводится из самих текстур (Sobel по яркости → tangent-space DataTexture, seamless) и наложена на meshStandardMaterial обеих InstancedMesh (normalMap + normalScale): `frontend/src/components/three/CubesHeroImpl.jsx`. Кредиты higgsfield не тратил — derived normal точнее AI-карты.
- Куча плотнее и выше: R/H в buildParticles → компактный вариант (gravel R2.5→2.1, H2.3→2.9; sand R3.1→2.7). Раскидистый вариант оставлен закомментированным рядом: `frontend/src/components/three/CubesHeroImpl.jsx`.
- Гашение канваса мягче: старт fade vh*1.0→vh*1.35, диапазон vh*0.6→vh*0.9 (куча дольше видна собранной): `frontend/src/components/three/CubesHeroImpl.jsx`.
- Читаемость заголовка (композиция hero): `frontend/src/components/three/CubesHeroImpl.jsx` + `frontend/src/styles.css`:
  - text-safe zone — эллипс SAFE по центру кадра; buildParticles пере-бросает точки кучи И рассыпа, чтобы частицы (гравий+песок) не лезли на текст, а обрамляли его (Particles теперь считает count = data.length);
  - скрим `.hero-scrim` — мягкий радиальный vignette var(--bg) поверх канваса и под текстом (внутри .kb-hero3d, гаснет вместе с ним);
  - калибровочные кубы — 7 шт., процедурная ч/б шахматка 4×4 (CanvasTexture, без внешних моделей), крупнее гравия, медленно вращаются, участвуют в scroll-скрабе, расставлены вне safe-zone;
  - контактная тень под кучей (drei ContactShadows, frames=1 при reduced-motion) — куча стоит на земле.
- Скролл резче + симметрия поля + читаемость заголовка: `frontend/src/components/SmoothScroll.jsx`, `frontend/src/components/three/CubesHeroImpl.jsx`, `frontend/src/styles.css`:
  - Lenis duration 0.6→0.5, lerp 0.12→0.09 (scroll-scrub прогресса уже привязан к scrollY напрямую, без dt-догоняния);
  - разлёт частиц по X привязан к аспекту вьюпорта (halfW = tan(fov/2)·z·aspect) → симметричное покрытие до обоих краёв на широком мониторе; выравнено распределение, не количество (камера/origin в x=0);
  - жёсткий «колодец» под заголовком: zoneKeep с внутренним эллипсом (inner=0.8·SAFE) где частиц НЕТ вообще + растушёванный feather к внешней границе;
  - подложка `.hero-title-glow` — плотный (var(--bg) 92%) радиальный градиент под блоком заголовка, z над скримом/под текстом, гаснет с канвасом, без видимой плашки;
  - гало на глифах: многослойный text-shadow цветом var(--bg) на h1, усиленный на золотом курсиве;
  - контраст золота: курсив #B87A18→#9A6410 (глубже охра, отрыв от кремового фона).
- Финал читаемости hero (расфокус фона за текстом): `frontend/src/styles.css` + `frontend/src/components/three/CubesHeroImpl.jsx`:
  - на слой скрима добавлен backdrop-filter: blur(10px) (+ -webkit-), ограниченный центральным эллипсом через mask-image → гравий под текстом уходит в дымку, по краям кадра остаётся резким; блюр гаснет вместе с канвасом (лежит в fade-обёртке), карточку ниже не задевает;
  - «колодец» без частиц расширен ~15% (SAFE ax 2.5→2.88, ay 1.45→1.67);
  - заголовок утяжелён на шаг: h1 600→700, курсив 500→600.
- History, верх страницы: `frontend/src/styles.css` + `frontend/src/pages/History.jsx`:
  - шапка «Карелия · Журнал замеров / История анализов» — убрана глухая залитая коробка (.kh-head__text прозрачный), eyebrow переведён в лёгкую стеклянную капсулу (стекло+бордюр, как .badge в hero), заголовок сидит прямо на берёзовых волнах;
  - пустое состояние: залитый тёмно-зелёный силуэт гуся заменён на тонкий контурный лайн-арт лебедя (fill:none, stroke currentColor, strokeWidth 2.4) — в один почерк с берёзами/медведями.
- Токен-слой + доступность (шаг 1 аудита, G1–G5 + A1–A3): `frontend/src/styles.css`, `frontend/src/pages/Profile.css`, `frontend/src/report-panel.css`:
  - G1 радиус-шкала --r-xs:8/--r-sm:10/--r:14/--r-lg:16/--r-pill:999, хардкод-радиусы разведены по токенам; единый радиус карточек = --r-lg (16), контролы/кнопки/дропзона/статус = --r (14);
  - G2 elevation-шкала --shadow-sm/md/lg (--shadow → алиас md), ~разрозненные hover/resting тени сведены к шкале;
  - G3 брейкпоинты — набор 560/768/1024/1200 как ориентир (существующие грид-коллапсы уже в этих точках);
  - G4 типографика — убраны половинчатые размеры (10.5/11.5/12.5/13.5 → ближайший шаг);
  - G5 контролы — единый --control-* (radius/pad/font 14/weight 400/focus ring) + --label-* eyebrow-спек; .field/.auth-field/.profile-field/.kb-field переведены на них, лейблы унифицированы (uppercase overline);
  - A1 --light как цвет ТЕКСТА → --muted (18+ мест), --light оставлен только для бордеров/hairline;
  - A2 .field input font-weight 300→400; A3 явное animation:none для .page/header/.badge-dot в reduced-motion.
  - Hero не тронут: единственная ссылка (.badge box-shadow) заменена 1-в-1 на --shadow-sm с тем же значением. Проверка целостности токенов: все var(--*) резолвятся; сборка зелёная.

- Калибровочный куб: новый `frontend/src/components/CubeSettings.jsx` — icon-button (Settings/UserCog из lucide-react) + сворачиваемая панель «Параметры калибровочного куба» (клеток на грани, взаимоисключающие поля квадрат/грань, live computed square_size_m в метрах, валидация N 2..10 / квадрат 3..60мм / грань 15..300мм, индикатор «изменено» + бейдж-точка + «Сбросить к стандартным»).
- `pages/Analyze.jsx`: подключён CubeSettings под кнопками, невалидный куб блокирует runAnalysis/addToQueue, блок `cube` прокинут в enqueue.
- `queue/queue.js`: `cube` сохраняется в item и уходит в payload вебхука как FormData-поле `cube` (JSON). Бэкенд/n8n не тронуты.
- Добавлены стили `.cube*` в `styles.css`; установлен `lucide-react`.
- CubeSettings: шестерёнка перенесена в пустой слот колонки «В очередь» (`.cube` → display:contents в сетке .actions, панель на всю ширину) — `Analyze.jsx`, `styles.css`
- CubeSettings: убрана блокировка полей, живая двусторонняя синхронизация «Сторона квадрата» ↔ «Длина грани» (грань=сторона×N, сторона=грань/N), lastEdited для пересчёта при смене N, visual pulse на пересчитанном поле — `CubeSettings.jsx`, `styles.css`
- CubeSettings: ошибки валидации очеловечены и вынесены под конкретное невалидное поле вместо общего дампа диапазонов — `CubeSettings.jsx`, `styles.css`
- CubeSettings: убрана текстовая подпись «Параметры куба» рядом с шестерёнкой (осталась только иконка-триггер); подтверждено, что блок cube {square_size_m, squares_per_side} уходит в payload обеих кнопок через enqueue→flushItem — `CubeSettings.jsx`, `styles.css`
- backend/analyses.py: n8n-payload теперь содержит блок "cube" {squares_per_side, square_size_m}. Эндпоинт POST /analyses/ принимает cube из FormData (его уже слал queue.js), нормализует с дефолтами (4 клетки, 0.0175 м), округляет square_size_m до 5 знаков и пробрасывает в _call_n8n_and_save
- Баг плашки «ОБЪЁМ» в History: плашка показывала top-1 из «🔗 Похожие в БД» (similarity), а не измеренный объём — `frontend/src/pages/History.jsx`:
  - причина: в схеме нет колонки объёма (`supabase/schema.sql` — только `result TEXT`), поэтому `getVolume` всегда падал в фолбэк-регэксп `/([\d.,]+)\s*м³/`, который цеплял ПЕРВОЕ «X м³» в тексте = строку «Похожие в БД». Бэкенд/n8n/receiver_dust3r.py возвращают верный `volume_m3`, порча — чисто на фронте при парсинге текста;
  - фикс: `getDust3rVolume` строго парсит «Объём DUSt3R: X м³»; `getVolume` = поле БД → DUSt3R → фолбэк с вычищенной строкой «Похожие в БД»; если объём не определён (partial/куб не найден) — плашка «не определён», а не чужое similarity-значение;
  - `fmtVolBadge` — адаптивная точность (малые объёмы 0.1249/0.0298 не схлопываются в «0»);
  - sanity-check `console.warn` при расхождении измеренного и generic-объёма на порядок;
  - backfill не нужен: объём считается на рендере из `result`, старые записи чинятся автоматически. Проверено на обоих примерах (80→0,125; 2→0,0298), сборка зелёная.
- Заголовок «Параметры калибровочного куба» по центру, удалён блок «→ сторона квадрата … передаётся на сервер» (CubeSettings.jsx, styles.css)
- Фикс крена 3D-моделей во вьюере (все реконструкции завалены ~на 45°): `frontend/src/components/plyAlign.js` (новый), `frontend/src/components/PlyViewerImpl.jsx`:
  - диагноз: крен ОДИНАКОВ у PLY и GLB, а GlbModel рендерился с rotation=[0,0,0] (identity) — значит крен НЕ от фиксированного поворота вьюера, а запечён в данных. Сырой DUSt3R пишет облако в системе опорной камеры (Y-вниз, Z-вперёд) без гравитации; т.к. пользователь снимает под ~одинаковым наклоном сверху, все сцены наследуют ОДИН И ТОТ ЖЕ крен. Пайплайн (dust3r_gpu.py) вне репозитория — фикс только во вьюере;
  - решение (root, не хардкод-поворот): plyAlign.js — RANSAC-оценка доминирующей плоскости земли из самих точек, разворот её нормали в +Y (нормаль ориентируется в сторону массы объекта). levelGeometry() для PLY (мутирует BufferGeometry до computeBoundingBox → центрирование/размеры/грид-пол в исправленной системе), levelObject() для GLB (собирает мировые вершины всех мешей, разворачивает корень сцены). Идемпотентно (guard userData.__leveled);
  - убран расходящийся хардкод: PLY rotation=[Math.PI,0,0] и GLB rotation=[0,0,0] → оба identity, ориентация консистентна между вьюерами. Сборка зелёная.
- Выравнивание 3D-моделей: up-вектор из пайплайна вместо самоугадывания (крен убран, но бочка вставала вверх дном — RANSAC ловил плоскость, а знак «вверх» по массе ломался на симметричных объектах):
  - Часть A (фронт): `frontend/src/components/plyAlign.js` — levelGeometry(geometry, up) / levelObject(root, up) принимают up-вектор [x,y,z]; если передан — выравнивают напрямую (quaternionFromUp), БЕЗ RANSAC и без эвристики знака. Фолбэк (нет up) — computeFallbackQuaternion: убирает только крен по доминирующей плоскости, знак канонический (n.y≥0), НЕ угадывает по массе (перевёрнутость допустима, крен — нет).
  - Проброс up: `PlyViewer.jsx`/`PlyViewerImpl.jsx` — props up (PLY-кадр) и upGlb (GLB-кадр); Scene выбирает по mode. `pages/History.jsx` — extractUp/extractUpGlb (JSON-поля up_vector/up_vector_glb или строка в тексте вебхука). `pages/Analyze.jsx` — парсинг up_vector/up_vector_glb из data и JSON + текстовый фолбэк, state upVec/upGlbVec, сброс в очистке.
  - Часть B (пайплайн, разведка + правка): `Analyz/dust3r_gpu.py` — plane_oriented[:3] (нормаль опорной плоскости, уже со знаком «объект на +стороне») = честный «вверх». PLY в сыром кадре → up_vector=(a,b,c); GLB повёрнут Rx(π) в pointcloud_to_mesh (flip Y,Z) → up_vector_glb=(a,-b,-c). Оба добавлены в result-dict; геометрия НЕ тронута (объём/старые записи не меняются). `Analyz/receiver_dust3r.py` — up_vector/up_vector_glb проброшены в возвращаемый result-dict (→ analyses.result, откуда парсит фронт). В dust3r_results НЕ пишем (нет колонок, иначе REST-insert упал бы).
  - Старые записи без up-вектора — фолбэк-RANSAC (крен убран, возможна перевёрнутость). Сборка vite зелёная, python компилируется.
- ФИКС белого экрана на здоровых анализах БЕЗ up-вектора: `frontend/src/components/plyAlign.js` — `quaternionFromUp(up)` при `up=null` уходил в ветку `up.clone()` → `null.clone()` → `TypeError: Cannot read properties of null (reading 'clone')` → падал весь three-стек (белый экран, id 1bb4a03b и любые записи без up_vector). Добавлен guard `if (!up) return null;` в начале функции → вызывающий (levelGeometry/levelObject) штатно уходит в RANSAC-фолбэк и модель реально отрисовывается. Точечная правка одной строки, логика расчёта/парсинга не тронута. Сборка зелёная (чанк PlyViewerImpl-CQedOlyb.js).
- [2026-07-22] ФИКС «старый бандл после деплоя / белый экран на выпиленных чанках»: `frontend/nginx.conf` — общий regex-локейшн `\.(js|css|...)$` вешал `Cache-Control: public, immutable; expires 30d` на ВСЁ, включая `sw.js` и `index.html`. Точки входа обновления кэшировались надолго → браузер после деплоя тянул старый index.html/SW, ссылавшиеся на уже удалённые `assets/*.js` → 404 → белый экран. Добавлены точные (=) location ВЫШЕ regex: `location = /sw.js { Cache-Control "no-cache" }` и `location = /index.html { Cache-Control "no-cache, must-revalidate" }` (точный location в nginx имеет приоритет над regex). Хешированные `assets/*.js` (хеш в имени) остались immutable — корректно.
- [2026-07-22] ФИКС бага офлайн-очереди PWA «после возврата сети фото не отправляются»: `frontend/src/queue/queue.js`.
  - Корень: `flushItem` выставлял статус `sending` ДО fetch. Офлайн запрос токена (supabase.auth.getSession → refresh) мог зависнуть → элемент застревал в `sending`, id — в inFlight; после возврата связи `flushAll` его пропускал (берёт только `queued`). Теперь `flushItem` при `!navigator.onLine` СРАЗУ возвращает, статус остаётся `queued` — залипнуть в `sending` офлайн больше нельзя.
  - `requeueOrphans()`: осиротевшие `sending` (не в inFlight — напр. после перезагрузки вкладки) → `queued`. Вызывается в `initQueue` и в начале `flushAll`.
  - `scheduleFlush()`: коалесценция триггеров online/visibilitychange/kb-flush/старт (1.8) — один отложенный флаш вместо 3-4 параллельных.
  - `ensureSafetyNet()`: пока в очереди есть неотправленное и мы онлайн — повтор флаша каждые 10 c; страхует от пропущенного события `online`. Гаснет, когда очередь пуста. Заводится в `enqueue` и `initQueue`.
- [2026-07-22] ФИКС 1.6: `frontend/src/pages/Analyze.jsx` — рекурсивный обход JSON-ответа (findUp/findUrls) в pollOnce получил потолок глубины (MAX_DEPTH=8) и ранний выход, как только оба искомых значения найдены. Меньше работы на каждом поллинге при большом ответе.
- [2026-07-22] ФИКС 1.5: `frontend/src/pages/History.jsx` — интервал поллинга больше не зависит от [items] (не пересоздаётся каждые 10 c): hasPending держим в ref, интервал создаётся один раз и сам решает, опрашивать ли. load() из подписки на очередь дебаунсится (400 мс) — при флаше пачки нет серии параллельных запросов к /analyses/.
- [2026-07-22] 1.7: `supabase/migration_client_id.sql` (новый) — идемпотентная миграция: ADD COLUMN IF NOT EXISTS analyses.client_id + частичный UNIQUE INDEX analyses_user_client_uniq (user_id, client_id) WHERE client_id IS NOT NULL. В schema.sql их не было; де-факто дубли и так режет PK на id (id==client_id), индекс приводит схему в соответствие с комментариями бэкенда.
- [2026-07-22] ФИКС 1.2: Error Boundary вокруг 3D-вьювера. Новый `frontend/src/components/ViewerErrorBoundary.jsx` (class, getDerivedStateFromError, локальный fallback «3D-модель недоступна», без onError-колбэков и без дёрганья родительских loading — чтобы не повторить баг с вечным спиннером). Обёрнут `<PlyViewer>` в `frontend/src/pages/Analyze.jsx` и `frontend/src/pages/History.jsx` (height=320px). Краш three.js (битая модель/WebGL context lost) больше не роняет всю страницу.
- [2026-07-22] ФИКС 3.3: `backend/app/routers/analyses.py` — подтверждено, что клиент supabase-py синхронный (`create_client` в supabase_client.py, вызовы .upload/.insert без await). Блокирующие вызовы в цикле загрузки фото (make_thumbnail-Pillow, storage.upload оригинала и миниатюры, colmap_photos.insert.execute) обёрнуты в `asyncio.to_thread` — event loop больше не блокируется на время загрузки пачки. Порядок обработки и структура ответа не тронуты; get_public_url оставлен синхронным (чистое построение строки, без сети).
- [2026-07-22] ФИКС: 3D-модели всегда были перевёрнуты вверх дном. `frontend/src/components/plyAlign.js` — в `computeFallbackQuaternion` знак нормали опорной плоскости теперь выбирается по массе облака: насыпь (щебень) существует только над землёй, поэтому нормаль ориентируется в сторону, где реально сосредоточена масса точек (средняя знаковая проекция на нормаль). При симметрии массы (|проекция| < 1% диагонали) — фолбэк на прежний канонический знак (Y ≥ 0). Раньше всегда брался канонический знак → насыпи вставали вверх дном.
- [2026-07-22] ФИКС (v2): модели всё ещё были полностью перевёрнуты — потому что up_vector из пайплайна применялся и был инвертирован, фолбэк не срабатывал. `frontend/src/components/plyAlign.js` — добавлена универсальная ПОСТ-проверка знака «вверх» ПОСЛЕ выравнивания в +Y (работает и для up из пайплайна, и для фолбэка): `needsFlip()` смотрит центр масс по Y — у насыпи широкое основание (масса внизу), узкий пик (мало точек вверху); если центр масс смещён в верхнюю половину bbox (> mid + 2% диапазона) → доворот на 180° вокруг X (`flipUpsideDownQuaternion`, Y→−Y, Z→−Z, плоскость остаётся горизонтальной). Применено в `levelGeometry` (PLY) и `levelObject` (GLB, проверка в мировых координатах). Сборка vite прошла.
- [2026-07-22] ОПТИМИЗАЦИЯ hero-3D-сцены (`frontend/src/components/three/CubesHeroImpl.jsx`, текстуры в `frontend/public/textures/`). Страница Analyze тормозила из-за scroll-scrubbed сцены сборки кучи (880 InstancedMesh-частиц + 7 кубов + ContactShadows).
  УЗКИЕ МЕСТА (аудит по коду): (1) `frameloop` дефолтный "always" + канвас `fixed inset:0` → 60fps рендер НАВСЕГДА, даже когда hero ушёл за скролл (opacity:0); (2) useFrame без dirty-check → 880 матриц пересчитывались и перезаливались в GPU каждый кадр даже в простое (844 800 float-загрузок/с); (3) `ContactShadows frames={Infinity}` → пересчёт soft-shadow map 60×/сек; (4) синхронный Sobel-normalmap (~0.5M px ×2) в useMemo на маунте блокировал первый paint; (5) текстуры 1024² PNG = 5.0 MiB; (6) 7 кубов = 7 draw calls; (7) dpr до 2.0 (4× пикселей на retina).
  ИЗМЕНЕНО: динамический `frameloop` "always"↔"never" по границе видимости (переиспользован scroll-fade, что рулит opacity) — вне видимости useFrame и рендер полностью стоят, авто-вращение замирает; при возврате продолжается с той же точки (позиции выводятся из scrollProgress(), состояние не сбрасывается). Dirty-check по scrollProgress: 880-инстансный буфер пересчитывается/грузится только когда скролл реально сдвинулся. `ContactShadows frames={1}` + ре-бейк по квантованному бакету скролла (`useScrollBucket`, ≤24 ремаунта на весь скролл вместо 60/сек). Normal-map перенесён в `requestIdleCallback` после первого paint (материал пересоздаётся по `key` при подъезде карты — иначе шейдер не перекомпилится). Текстуры → WebP 512 (`gravel.webp`/`sand.webp`). `dpr={[1,1.5]}`. CalibrationCubes → один InstancedMesh (7→1 draw call), вращение через аккумулируемый кватернион.
  МЕТРИКИ до→после (детерминированы из кода; методология — покадровая модель нагрузки, не live-DevTools: браузерная автоматизация в окружении не установлена):
   • Текстуры: 5 247 616 B PNG → 221 182 B WebP (−95.8%); PWA-precache −~5 MiB (старые PNG удалены).
   • Draw calls кубов: 7 → 1. Общие draw calls сцены ~10 → ~4.
   • Instance-буфер (float-загрузки/с, сцена видна, БЕЗ активного скролла): 880×16×60 = 844 800/с → 7×16×60 = 6 720/с (−99.2%). При активном скролле — только пока прогресс меняется.
   • ContactShadows re-bake: 60/с → 0 в простое, ≤24 на весь проход скролла.
   • Сцена ушла за скролл (opacity:0): было 60fps бесконечно → стало 0fps / 0 draw calls / 0 useFrame.
   • Fill-rate (dpr): относительно 2.0²=4.0 → 1.5²=2.25 (−44% пикселей на retina).
   • Маунт: Sobel-normalmap (~0.5M px ×2) убран с критического пути первого paint в idle.
  Анимация «сборки» визуально не изменена (тронуты только КОГДА/СКОЛЬКО считаем кадр, вес текстур и число draw calls). Рекомендуется подтвердить live DevTools Performance (6× CPU throttle, mobile) на реальном девайсе. `vite build` зелёный.
- [2026-07-22] ФИКС лага первого входа (маунт hero-сцены; аудит п.#4). Диагноз (подтверждён по коду): main thread на маунте забит одномоментно — (а) компиляция PBR-шейдеров при первом рендере сцены, (б) ВТОРАЯ перекомпиляция когда доезжает normal-map и материал пересоздавался по `key`, (г) канвас монтировался синхронно со страницей и конкурировал с формой загрузки. Прокрутка/рантайм не тронуты.
  ИЗМЕНЕНО (`frontend/src/components/three/CubesHeroImpl.jsx` + `frontend/src/styles.css`):
   • #2 — убрана вторая компиляция шейдера: добавлена модульная плоская 1×1 normal-заглушка `FLAT_NORMAL` (RGB 128,128,255). Материал частиц теперь ВСЕГДА создаётся с `normalMap={normalMap || FLAT_NORMAL}` и БЕЗ `key` → `USE_NORMALMAP` определён с первой компиляции, реальная Sobel-карта позже просто подменяет текстуру в слоте (обе non-null → тот же program cache key, без перекомпиляции). Раньше `key={normalMap?'with-normal':'flat'}` пересоздавал материал → второй long-task.
   • #1 — отложенный маунт канваса: `<Canvas>` монтируется не синхронно, а после первого paint формы через `requestAnimationFrame → requestIdleCallback` (стейт `canvasMounted`). Компиляция шейдеров уходит с критического пути входа; форма интерактивна сразу. Обёртка `.kb-hero3d-canvas` даёт CSS fade-in (`kbHeroFadeIn` 0.5s), чтобы появление сцены на кадр-два позже не выглядело резким (reduced-motion — без анимации).
  НЕ делал (по указанию «не переусложнять»): #3 `gl.compile()` — избыточно после отложенного маунта (единственная оставшаяся компиляция и так вне критического пейнта). #4 раскладка частиц `buildParticles` (~880×~20 итераций трига, <1мс) не long-task + теперь считается в idle вместе с отложенным маунтом.
  РЕЗУЛЬТАТ: компиляций шейдера частиц 2→1, и она вынесена за первый paint. `vite build` зелёный. Подтвердить long-task на входе рекомендуется live DevTools Performance (жёлтые блоки Compile/Evaluate при загрузке).
- [2026-07-22] По просьбе: поворот 3D-моделей на 180° вокруг оси Y ТОЛЬКО во вьюверах страницы истории. Добавлен проп `yaw180` (default false), проброшен `PlyViewer.jsx` → `PlyViewerImpl.jsx` → `Scene`; применяется обёрткой `<group rotation-y={yaw180 ? Math.PI : 0}>` вокруг модели (модель центрирована в 0,0,0 → поворот на месте, камера/грид/гизмо не затронуты). Включён в `frontend/src/pages/History.jsx` (оба вьювера — glb и ply). `Analyze.jsx` не тронут. `vite build` зелёный.
- [2026-08-01] Резче скролл по всему сайту: `frontend/src/components/SmoothScroll.jsx` — Lenis `lerp 0.09 → 0.28`, убрал `duration/easing`, добавил `wheelMultiplier: 1.05`. Скролл стал почти нативным/чётким (была жалоба «плавает, неудобно»), скролл-скраб героя не дёргается.
- [2026-08-01] Публичный ЛЕНДИНГ. Новый `frontend/src/pages/Landing.jsx` + `frontend/src/landing.css` (всё скоупится под `.kb-landing`). Секции: 3D-hero (реюз `CubesHero`), marquee, счётчики-stats, «как это работает» (draw-in линия), tilt-карточки возможностей, showcase-цитата с параллаксом контурного мотива, финальный CTA, подвал. Анимации на `motion/react`: построчный выкат заголовка, магнитные кнопки, tilt+блик, счётчики (`animate`), параллакс (`useScroll/useTransform`), whileInView-ревилы. Учтён `prefers-reduced-motion`.
- [2026-08-01] Роутинг под лендинг (`frontend/src/App.jsx`): `/` → публичный `Landing` (lazy), приложение переехало `/` → `/app` (под `PrivateRoute`). Обновил внутренние ссылки: `Login.jsx` `navigate('/')→'/app'`, `Layout.jsx` (логотип, десктоп-нав, таб-бар `NAV.to`) `'/'→'/app'`. CTA лендинга ведёт в `/register` (гость) или `/app` (вошедший). `vite build` зелёный.
- [2026-08-01] РЕДИЗАЙН лендинга под скилл `impeccable` (методология применена вручную: прочитаны craft-floor/new-work/animate/typeset). Новый визуальный мир «ПРИБОР» (Persuade, бриф закреплён → concept-roll не запускался). Полностью переписаны `frontend/src/pages/Landing.jsx` и `frontend/src/landing.css`: графитовый near-black `#08090A`, liquid-glass хром (`.lg`/`.lg-strong` с градиентным hairline-кантом через mask-xor), ОДИН сигнал survey-оранжевый `#ff6a2b`. Типографика: Archivo (гротеск, весь текст) + JetBrains Mono ТОЛЬКО для реальных измерений (объём/масса/точки/координаты). Секции: hero (dbl-слой: генерируемый backdrop + процедурное облако точек), процесс-секвенция, панель-ОТЧЁТ (вместо hero-metric), spec-sheet с dotted-leader (вместо карточек-сетки), кино-момент с параллаксом, финал, подвал. Соблюдён craft-floor: убраны gradient-текст, eyebrow-над-заголовком, одинаковые icon-карточки, жёсткие тени. Контракт направления — в шапке Landing.jsx.
- [2026-08-01] Новый компонент `frontend/src/components/three/PointCloudLandingImpl.jsx` — процедурное WebGL-облако точек (focal-motion: реконструкция «фото→насыпь» по скроллу). Перф по CLAUDE.md: гейт frameloop по видимости, dirty-check по scroll-progress, dpr[1,1.5], отложенный маунт, заморозка при reduced-motion. AdditiveBlending, ~6000 точек, ~9% сигнально-оранжевые «линии скана».
- [2026-08-01] Сгенерирован hero-ассет через Higgsfield (nano_banana: облако-скан карьера, 2 кредита, осталось 2) → `frontend/public/landing/hero-cloud.webp` (95 КБ; исходный png удалён из public ради precache). Motion: BlurText-заголовок, clip-path scan-wipe ревилы, счётчики-измерения, параллакс — разные материалы, не одинаковый fade на каждой секции. `vite build` зелёный, Landing 21 КБ / CSS 12 КБ.
- [2026-08-01] РАЗВОРОТ по фидбэку: тёмный «прибор» забракован — лендинг вернули в ГАММУ ОСНОВНОГО САЙТА (кремовый/лес/охра, Cormorant + Onest), но с премиум-подъёмом: light liquid-glass + бенто-дашборд (construction-planning референсы) + много калибровочных кубов/гравия. Переписаны `frontend/src/pages/Landing.jsx` и `frontend/src/landing.css` (мир `.kb-landing`). Секции: hero (реюз `CubesHero` — гравий+песок+кубы) + glass-чипы замеров; бенто-дашборд (виджеты разного размера: объём/масса/плотность/погрешность + мини-конвейер + экспорт-чипы); WebGL-поле кубов; процесс (3 шага); кино-момент; финал (зелёная glass-панель); подвал. Focal-motion — сборка кубов/гравия.
- [2026-08-01] Новый `frontend/src/components/three/CubeShowcaseImpl.jsx` — ~34 калибровочных куба, собираются в пирамиду по позиции секции (self-contained, IntersectionObserver-гейт frameloop, dpr[1,1.5], отложенный маунт). Удалён неиспользуемый `PointCloudLandingImpl.jsx`. Сгенерирован кремовый still-life кубов+гравия → `public/landing/build-still.webp` (80 КБ; тёмный hero-cloud удалён). Higgsfield-кредиты исчерпаны (4→0). `vite build` зелёный: Landing 21 КБ / CSS 14 КБ / CubeShowcase 3.3 КБ.
- [2026-08-01] 3D-ОБЛЁТ вместо статичного hero (технология из NovaAI-референса, но реальными моделями). Пользователь накачал GLB в `/3dmodels`. Сжал через `gltf-transform optimize` (quantize+webp, нативно грузится three.js без Draco): sand 2.9М→27КБ, pile_of_gravel 78М→657КБ, construction_site 79М→4.2М (simplify error .008) → `frontend/public/models/{sand,gravel,site}.glb`. stone_gravel (553К верш.) пропущен как избыточный.
- [2026-08-01] Новый `frontend/src/components/three/ConstructionOrbitImpl.jsx` — fixed-канвас на весь лайфтайм: песок+гравий (инстансы GLB) + процедурные шахматные кубы ОБЛЕТАЮТ site.glb, scroll-scrubbed (глобальный прогресс скролла, сглажен lerp 0.08), на скролле стягиваются к центру; камера водит по орбите. Перф: гейт frameloop по `document.hidden`/reduced-motion, dpr[1,1.5], отложенный маунт, 3 инстанс-меша. Landing.jsx: орбита смонтирована в корне (`.kb-l-orbit` fixed z0), секции полупрозрачные (rgba .93) + прозрачные reveal-полосы `.kb-l-band` между секциями (объекты видно целиком, как 80vh-спейсер NovaAI), больше стекла (blur 14/30px). Удалён `CubeShowcaseImpl.jsx`. `vite build` зелёный: ConstructionOrbit 5.2 КБ, модели по требованию.
- [2026-08-02] HERO лендинга заменён на ФИЗИЧЕСКУЮ анимацию (план утверждён в plan mode). Новый `frontend/src/components/three/MaterialFlowHeroImpl.jsx`: материал (песок+гравий) распределён вокруг процедурного здания → организованным потоком облетает его → реально пересекает проём ворот → падает внутри → из прилетающих частиц формируется куча (height-field 44×44 с углом естественного откоса + скатывание по склону). Фазовый автомат частицы SUPPLY→ORBIT→APPROACH→INSIDE→DEPOSIT→(rest)→SUPPLY (time-driven, steady-state рецикл: приход≈уход, куча растёт из пустоты и держится). Здание — процедурное тёмное стекло + охряные рёбра + открытые ворота (не site.glb — ради точных проёма/пола/зоны кучи). Перф: 2 InstancedMesh (~2400 песок + ~800 гравий, вдвое на мобиле), CPU-физика по типизированным массивам, frameloop-гейт по видимости/document.hidden, dpr[1,1.5], отложенный маунт, reduced-motion → статичная камера/куча.
- [2026-08-02] Landing.jsx/landing.css под новый hero: убрана full-page орбита (`ConstructionOrbitImpl.jsx` удалён) и reveal-полосы `.kb-l-band`; hero стал ТЁМНЫМ (twilight-градиент лес→графит, «чуть больше тёмных тонов»), заголовок/сабтайтл светлые, `.kb-l-flow` absolute в hero; nav переключается свет↔тьма (`is-solid` на скролле: светлый логотип над тёмным hero → тёмный + кремовый бар на контенте); нижние секции возвращены к сплошному крему. Смысл hero теперь ОТЛИЧАЕТСЯ от страницы анализа (там — сборка кучи по скроллу). `vite build` зелёный: MaterialFlow 7.7 КБ, тяжёлые GLB не грузятся.
- [2026-08-02] HERO переделан на РЕАЛЬНЫЕ модели из `/3dmodels` (бриф: только эти модели, БЕЗ прореживания полигонов). Инвентаризация 4 GLB: block_sand_rock (18МБ, 322 верш., 3×4K), stone_gravel (43МБ, 553К верш., 8K), pile_of_gravel (74МБ, 2К верш., 3×4K), construction_site_tower_crane (82МБ, 965К верш., 8K). Сжаты gltf-transform БЕЗ simplify: `resize 4096 → etc1s (KTX2) → meshopt`. Веса: 18→5.4, 43→8.0, 74→8.2, 82→11 МБ (итого ~225→~33 МБ, ~14%). Полигоны целы (EXT_meshopt_compression + KHR_mesh_quantization), текстуры KHR_texture_basisu ETC1S 4K (gpuSize ~11МБ, сжато в VRAM). KTX-Software (toktx v4.4.2) поставлен локально из GitHub-релиза (в brew формулы `ktx` нет) в `~/.ktxtools` (pkg распакован без sudo); basis-транскодер в `frontend/public/basis/`.
- [2026-08-02] Новый `frontend/src/components/three/MaterialShowcaseHeroImpl.jsx` + `ktx2gltf.js` (useGLTF: draco off, meshopt on, KTX2Loader через extendLoader, транскодер локально `/basis/`). Концепция «сырьё→замер→стройка», time-driven: Акт1 камень+гравий+песок влетают/вращаются → Акт2 габаритный бокс + размерная сетка + Html-подпись «V≈1428 м³·2271 т» (замер, служебные примитивы) → Акт3 стройплощадка с башенным краном поднимается, кран крутится, камера оседает. drei Environment (Lightformer, процедурно — higgsfield=0 кредитов) + ContactShadows, тени вкл., dpr=[1,2], AdaptiveDpr, useProgress-лоадер, reduced-motion → статичный кадр Акта2. Заменил `MaterialFlowHeroImpl.jsx` (удалён). `vite build` зелёный: hero-чанк 5.4 КБ (модели грузятся лениво отдельно, бандл не раздут).
- [2026-08-02] HERO переведён на SCROLL-DRIVEN концепцию (по просьбе): `MaterialShowcaseHeroImpl.jsx` переписан — по scrollY здание `site_crane` ПРИБЛИЖАЕТСЯ (камера наезжает), на его земле СОБИРАЕТСЯ КУЧА из гравия+песка+камня (модели растут 0→1 со stagger), рядом падает и встаёт ПРОЦЕДУРНЫЙ шахматный калибровочный куб, наверху прорисовывается габаритный бокс + подпись «V≈1428 м³·2271 т». scrollP()=scrollY/(vh·1.1). Модели те же сжатые (meshopt+KTX2), процедурный куб — служебный референс масштаба. Пофикшено двойное масштабирование (Model нормализует к target, group растёт 0→1). `vite build` зелёный, hero-чанк 5.9 КБ.
- [2026-08-02] Фидбэк по скриншоту: (1) кремовая nav-плашка `is-solid` поверх тёмного hero → сделал ТЁМНОЙ (`rgba(11,15,8,.86)`, логотип/ссылки светлые) в `landing.css`. (2) scroll-driven hero уезжал вверх при прокрутке → «не видно анимации»: перевёл `MaterialShowcaseHeroImpl` на САМОВОСПРОИЗВОДЯЩИЙСЯ time-driven луп (CYCLE 12с: сборка→удержание→сброс→повтор, аккумулятор dt без скачка при паузе). Всё видно без скролла: здание наезжает, куча собирается, куб падает, замер. Пофикшен drei `<Html>` — DOM-подпись замера не скрывалась с родителем (гашу через ref opacity/visibility). `vite build` зелёный, hero-чанк 6.1 КБ.
- [2026-08-02] HERO переделан на ВИХРЬ (по референсу пользователя): `MaterialShowcaseHeroImpl` — тысячи инстансов гравия+песка (~1600+2200, текстуры реального гравия/песка `public/textures/*.webp`) закручиваются ВИХРЕМ (θ+=ω, r↓, y↓ — коилятся к центру) и формируют РАСТУЩУЮ НАСЫПЬ (height-field 40×40, угол откоса), steady-state рецикл. Top-down изометрия (камера [~sin·12.5, 11, ~cos·12.5]→(0,.9,0), лёгкий дрейф). Рядом процедурный шахматный куб (вращается), сзади детальная стройплощадка `site_crane` (meshopt+KTX2), пыль в свете (Points дрейфят вверх), грунт-пол. Перф: тени частиц ВЫКЛ (их тысячи; контакт даёт ContactShadows), shadow-map 1024, dpr[1,2], frameloop-гейт, reduced-motion → оффлайн-досыпка кучи. AR-маркеры (Объём/Масса/Точность жёлтые овалы + бейдж «Динамическая сборка») — 2D-оверлей hero (drei Html ушёл бы под скрим). `vite build` зелёный, hero-чанк 8 КБ.
- [2026-08-02] Два фикса по скриншоту. ФИКС 1: верхняя nav-плашка была кремовой наверху — глобальный `header{background:rgba(244,240,232,.94)}` из `styles.css` применялся к `<header class="kb-l-nav">`. Перебил на СТЕКЛО в `.kb-l-nav` (`background:transparent` + `backdrop-filter:blur(14px) saturate(1.2)` + hairline-бордер; класс бьёт голый `header` по специфичности). `is-solid` (скролл) сделал полупрозрачным стеклом (alpha .86→.55). Тему/app не трогает (скоуп `.kb-landing`). ФИКС 2: убрал рой инстансов (Vortex/Dust — «летающие многоугольники»). `MaterialShowcaseHeroImpl` переписан на КИНО-ДРОН: камера аэрофото→снижение+наезд на площадку с покачиванием→посадка+зависание (time-driven, smootherstep, sway↓ по ходу); детальные модели `/3dmodels` (гравий+песок+камень) СЛЕТАЮТСЯ в ОДНУ кучу (lerp from→heap, scale 0→1, stagger) и замирают; калибровочный куб падает вплотную; здание site_crane — окружение на земле; grunt-пол; ContactShadows; reduced-motion→статичный собранный кадр. Пофикшен баг `useRef()` в `useMemo` (вынес рефы наверх). Подписи Объём/Масса/Точность подтянуты к куче (кластер у центра, не по краям), скрыты на мобиле. Тени частиц не нужны (частиц нет). `vite build` зелёный, hero-чанк 5.2 КБ.
- [2026-08-02] Донастройка hero (структуру не менял). КАМЕРА: финал у земли и вплотную — pos [1.8,0.85,4.6], look [0.1,2.6,-2.5], FOV 40, near 0.25, y-clamp ≥0.55 (без клипа пола), ракурс снизу вверх на кучу+кран. КУЧА: теперь МНОГО ИНСТАНСОВ геометрий из /3dmodels (извлекаю geo+mat из GLB, нормирую max-dim→1, InstancedMesh) — block_sand_rock ×460 (0.30–0.52) + pile_of_gravel ×55 (0.5–0.85) + stone_gravel ×8 (0.6–1.0, валуны; больше нельзя — 553K верш.), упаковка в конус R1.9/H2.4 (packCone), слетаются к центру и укладываются плотно, потом замирают (микро-дрейф 0.006). Тени инстансов ВЫКЛ (сотни; контакт — ContactShadows), receiveShadow вкл; тени только у здания/куба. КУБ: SIZE 0.9→0.48 (~H/5), вплотную к куче. Мобилка: инстансы вдвое. Подписи опущены (низкий кадр). `vite build` зелёный, hero-чанк 6.6 КБ.
- [2026-08-02] Финальная донастройка hero. Проблема по скринам: `site_crane` (большой фотограмметр-скан участка) стоял передним краем z≈+2.7 ПЕРЕД кучей (z≈0.2) → камера утыкалась в его фасад. ФИКС КАМЕРЫ: здание отодвинул `[0.4,0,-3.2]`→`[0,0,-9.5]`, target 12→13 (перед. край ~-3.6, целиком за кучей); камера-финал pos [3.0,1.6,5.4], look [0,1.4,-1.5] (куча в центре, кран сзади), y-clamp ≥0.9, near 0.3 → без клиппинга. ФИКС ЧАСТИЦ: мельче ~/15 и кратно больше — block_sand_rock ×8000 (0.020–0.036), pile_of_gravel ×1200 (0.03–0.06), stone_gravel ×5 (0.03–0.05); `packCone` теперь пакует «шкуру» конуса (r≈0.7–1.0·maxR, объём R1.9/H2.4 прежний) и сыплет инстансы СВЕРХУ на свои места (укладка, не рой). ~9200 инстансов, тени инстансов ВЫКЛ. Куб 0.48 не тронут. `vite build` зелёный, hero-чанк 6.6 КБ.
- [2026-08-04] ЛЕНДИНГ переведён на новый прототип (визуал 1:1, без потери инженерных преимуществ). ХИРО: 3D-сцену заменил на fullscreen-видео `public/landing/video.mp4` (autoPlay/loop/muted/playsInline/preload=auto, IntersectionObserver play/pause по видимости хиро, poster=build-still, reduced-motion→пауза на постере) + скрим над видео (`--hero-scrim:.2`). Раскладка хиро = GRID-полосы (`height:100svh; overflow:hidden; box-sizing:border-box; grid-template-columns:minmax(0,1fr); grid-template-rows:auto minmax(0,1fr) auto auto`) — гарантирует `scrollHeight===clientHeight` на любой высоте; типографика центральной полосы на container-queries (`container-type:size`, `min(cqi,cqh,px)` без нижних `clamp`-полов). КРИТ-ФИКС: без `grid-template-columns:minmax(0,1fr)` неявная auto-колонка сайзилась по max-content самой широкой полосы (чипы ~600px) → горизонтальное переполнение на узких экранах. Чипы показаний — 4-я полоса грида (в потоке, flex-wrap), стекло на тёмной подложке. Проверено headless-Chrome + in-page-пробой на 360/390/500/640/1280/1440 — heroH=0, heroV=0, doc-x=0, worstCard=0 везде. Файлы: `frontend/src/pages/Landing.jsx`, `frontend/src/landing.css`.
- [2026-08-04] ЛЕНДИНГ, бенто+стекло. БЕНТО переведён на вложенный флекс (БЕЗ `grid-column:span`): `.kb-l-bento__row` (recon flex:2 1 320 + metrics flex:3 1 420, оба min-width:0) → metrics = `grid repeat(auto-fit,minmax(150px,1fr))`; `.kb-l-bento__row--wide` = `repeat(auto-fit,minmax(280px,1fr))`. Счётчики (`Metric`) стартуют по `useInView(amount .35, once)` от 0 за 1.5с ease-out-cubic, reduced-motion→сразу финал. Полосы «Конвейера» scaleX 1/.82/.68/1 в той же волне. Карточка реконструкции — фон build-still + стеклянный бейдж «МЕШ ГОТОВ» (зелёная точка). Единое усиленное стекло `.kb-l-glass` на `.kb-l-w`/`.kb-l-engine__panel`/`.kb-l-step`; под стеклом — дрейфующие орбы (`.kb-l-orb` kbDrift) в светлых секциях; нав — sticky-стекло (гашу глобальный `header{animation:slideDown}`); кино-момент — текст в стеклянной панели; флора `decor/flora-*.png` в «Движке» (opacity .28, kbFloat). Медиазапросы (900/560) сохранены. Роуты `/register`,`/login`,`/privacy` и framer-motion reveal — на месте.
- [2026-08-04] ЛЕНДИНГ, 3D «Движка» — сцена перенесена из хиро в секцию «Движок» (lazy+Suspense, three.js отдельным чанком vendor-three, не в главном бандле) + починены реальные баги `MaterialShowcaseHeroImpl.jsx`: (1) `useInstGeo`→`bakeGeometryFromScene` ОБЪЕДИНЯЕТ все меши модели (stone_gravel: 9 мешей, раньше брался 1) через `mergeGeometries` с безопасным фолбэком на largest-меш (консоль чистая); (2) `pile_of_gravel` (ЦЕЛАЯ куча) 1200→6 инстансов (были «кучи в куче»), stone_gravel 5→7 акцентов, разные scale-диапазоны; (3) ContactShadows `frames={1}` + ребейк по кванту прогресса (≤15 раз), а не покадрово; (4) drift ЗАТУХАЕТ (×(1-s)) + `doneRef` — после укладки матрицы инстансов больше не пересобираются (раньше жгли буфер вечно); (5) видимость гейтится IntersectionObserver'ом на обёртке (работает в секции посреди страницы, а не только вверху). Сохранены KTX2/MeshoptDecoder, AdaptiveDpr, dpr[1,1.5], мобильные пороги. `vite build` зелёный: Landing 24КБ, MaterialShowcase-чанк 7КБ, three в vendor-three (не в index).
- [2026-08-04] ЛЕНДИНГ, правки по двум скринам + оптимизация. (1) БЕНТО-метрики 4-в-строку → 2×2 «вытянуто по диагонали»: `.kb-l-bento__metrics` = `grid repeat(2,minmax(0,1fr))`, левая колонка `translateY(-16px)`, правая `+16px`, `padding-block:18px` резервирует сдвиг (не вылезает за блок), на ≤560px диагональ выключена. (2) ХИРО «больше стекла» по прототипу (`landing.css`): чипы показаний — тёплое ЗОЛОТОЕ стекло с точкой `::before` и золотым serif-значением (разделители `·` убраны), бейдж/нав/ghost-кнопка — сильнее blur/прозрачнее (видео просвечивает), бейдж-текст → «Замер по фото прямо на площадке · Карелия». (3) ОЧЕНЬ МЯГКИЕ переходы: каждая секция красится градиентом, деля цвет со соседом (`#dash` bg→bg-2, `.kb-l-engine` bg-2→bg, proc/cine — единый bg, `.kb-l-final` bg→bg-2) → границ не видно; Lenis смягчён `lerp 0.28→0.1` (`SmoothScroll.jsx`); `scroll-margin-top:var(--nav-h)` на секциях. (4) 3D МАКС.ОПТИМИЗАЦИЯ (`Landing.jsx` Engine + `MaterialShowcaseHeroImpl.jsx`): сцена монтируется по близости (IntersectionObserver rootMargin 500px) — до подъезда ни байта three/GLB; на reduced-motion 3D не грузится вовсе; убраны stone_gravel+pile_of_gravel (~17МБ, лишние draw-calls), block_sand_rock 8000→4000 (моб 1600), site_crane только десктоп, dpr [1,1.5]→[1,1.25] (моб [1,1]), antialias/shadows off на мобиле, shadow-map 2048→1024, ContactShadows 1024→512. Проверено headless-Chrome на 360/390/1280: heroH/heroV/worstCard=0, PASS; `vite build` зелёный, three в vendor-three (не в главном бандле).
- [2026-08-04] ЛЕНДИНГ, кинематографичный переход Hero → «Показания» (CSS-first, без JS). Было «видео → почти прямая граница → cream». Стало «ночная стройка → холодный teal → тёплый янтарный свет → песочный haze → cream». В `Landing.jsx` после скрима добавлены два aria-hidden слоя `.kb-l-hero__blur` + `.kb-l-hero__fade` (оба z:1 под контентом z:2, absolute → grid/heroV не трогают). В `landing.css`: (1) скрим освобождён от низа — его линейка теперь гаснет в `transparent` к 82% (сверху только читаемость заголовка); (2) `.kb-l-hero__blur` — умеренный `backdrop-filter:blur(5px)`, включается маской только к низу (`mask-image` transparent 30%→#000 74%) — сцена уходит в дымку, верх резкий; (3) `.kb-l-hero__fade` (height 64%) — НЕ один linear: 2 radial (тёплый янтарный блум снизу `at 50% 112%` + холодный teal-шаг `at 50% 34%`) + вертикальная растяжка teal→амбер→песок→`var(--bg)` (низ ПЛОТНЫЙ cream → бесшовный стык с секцией); (4) `#dash` получил верхний радиальный янтарный отблеск (`at 50% -8%`) — дымка продолжается ИЗ hero в начало секции, стык не «обрывается». Reduced-motion не тронут (слои статичные, видео уже на постере). Проверено headless-Chrome 1280/900/640/500/390/360: heroH=0, docx=0, резкой границы нет; `vite build` зелёный.
- [2026-08-05] ЛЕНДИНГ, soft/glassmorphism 2026 (микродетали + матовое стекло, БЕЗ смены структуры/палитры). `landing.css`: (1) `.kb-l-glass` переведён на матовый фрост — заливка 56→30%%, blur(22) saturate(1.5), светлая рамка 1px, мягкая внутр.подсветка + подъём тенью (0 24px 56 + 0 4px 14 + 0 1px 2), `overflow:hidden; isolation:isolate`; редкий диагональный блик `::after` (`kbGlint` 11s, движение ~первые 12%% цикла → дальше покой, рассинхрон по `nth-child`); такой же блик на карточке реконструкции `.kb-l-w--recon::after`. (2) ПЛЁНОЧНОЕ ЗЕРНО `.kb-l-grain` (fixed, z:70 под навом, feTurbulence data-uri, `mix-blend-mode:soft-light` 5%%) — гасит бандинг на градиентах/стыке тёмный герой→cream; div добавлен в корень `Landing.jsx`. (3) ЗОЛОТОЙ SHIMMER на акцентных ем (`background-clip:text` + `kbShimmer` 6s): раздельные градиенты для тёмного фона (герой/финал — яркий кремово-золотой блик) и светлого (h2/кино — золото-в-золоте, без белого стопа → читаемо на креме). (4) МЯГКИЕ ДИАГОНАЛЬНЫЕ ШВЫ между секциями `#proc/.kb-l-cine/.kb-l-final::before` — широкая размытая тёплая полоса под наклоном (не линия). (5) КОНВЕЙЕР: бегущий блик по дорожке `.kb-l-mini__bar::after` (`kbFlow` 2.8s, каскад по шагам = поток Снимки→Облако→Меш→Объём). (6) Пульс зелёной точки «МЕШ ГОТОВ» (`kbDotPulse` 2.6s), левитация картинки-кубов `.kb-l-recon-img` (`kbLevitate` 7.5s, `inset:-3%%` прячет край). `Landing.jsx`: АТМОСФЕРА ГЕРОЯ — `HeroAtmosphere` (22 звезды `kbTwinkle` + 16 пылинок-боке `kbDustRise`, позиции стабильны через `useMemo`), 2 слоя глубины с курсорным ПАРАЛЛАКСОМ (`useMotionValue`→`useSpring`→`useTransform`, `onPointerMove` на секции, нормировка -1..1); монтируется только вне reduced-motion. ФИКС бага: золотая строка героя «из простых фотографий» пропадала — `background-clip:text` не клипит сквозь пословные спаны `BlurText`; переведена на `motion.em` с ПРЯМЫМ текстом + blur-in на всю строку. Все новые анимации медленные (2.6–13s) ease-in-out и выключены в `@media (prefers-reduced-motion)`. Проверено headless-Chrome (puppeteer-core→системный Chrome, 1512@2x) по всем секциям; `npm run build` зелёный (70 модулей, 5.5s). NB: локально не было `frontend/.env.local` (SPA падал `supabaseUrl is required`) — создан с placeholder-значениями (gitignored) для локального рендера; для рабочего auth подставить реальные Supabase-креды.
- [2026-08-05] ЛЕНДИНГ, HERO: фон-видео заменён на эталонный тёплый кадр `ДанныеДляЛендинга/preview_For_Hero.webp`. Исходник — это полный МОК (в нижней половине запечены дашборд-заголовок «Показания как на пульте» + карточки, а в кадре — нав/кнопки/чипы), поэтому взял из него ЧИСТУЮ сцену `dwebp -crop 0 86 1512 240` (без нава/кнопок/чипов/дашборда → нет двойных UI-призраков) → `frontend/public/landing/hero.webp` (95КБ). В `Landing.jsx` `<video>` (+ его IntersectionObserver play/pause и `vidRef`) убран, вместо него `<img.kb-l-hero__photo fetchpriority=high>`. В `landing.css`: (1) фото — резкая ПОЛНОШИРИННАЯ лента по центру (`left/top:50% translate(-50%,-50%); width:168%`), мягко растворяется сверху/снизу маской (`mask linear-gradient` 17%…80%) в тёплый градиент героя; на ≤760px — `object-fit:cover` заливка (центр-рабочий). (2) Фон/фолбэк героя — тёплый градиент изумруд→золото→крем (убран прежний тёмно-зелёный фолбэк; синего/teal нет). (3) `__scrim` — БОЛЬШЕ НЕ на всю высоту: только локальная полоса под навбаром + мягкий эллипс под заголовком (белый текст читается, остальной кадр в полную силу). (4) Растворение `__blur`/`__fade` удлинено и ПОТЕПЛЕНО: убран teal-radial, только золото→сэнд→крем, зона перехода ~25% высоты (`height:46%`, opaque к 86%) — стык с секцией «Показания» без границы. Звёзды+золотая пыль (`HeroAtmosphere`) остались поверх фото (тёплые, не синие). Проверено headless-Chrome (десктоп 1512, mobile 390 cover, стык hero→cream) + `npm run build` зелёный. NB: `public/landing/video.mp4` (2.4МБ) больше не используется — можно удалить.
- [2026-08-05] ЛЕНДИНГ, секция «Движок»: декоративная сцена (кран-GLB ~11МБ + 4000 PBR-инстансов гравия/песка + запечённые тени/Environment) ЗАМЕНЕНА на РЕАЛЬНЫЕ облака DUSt3R — буквальная демонстрация «того же движка, что в сервисе». Новый `frontend/src/components/three/EngineCloudImpl.jsx`: медленно вращающееся фотореалистичное облако точек (vertex-цвета) за стеклом, ориентация «вверх» тем же `plyAlign.levelGeometry` (RANSAC-плоскость + масс-проверка), центр+нормировка радиуса под фикс.камеру, туман в кремовый фон для глубины, fade-in материала. МАКС.ОПТИМ под «минимум нагрузки»: (1) грузится ТОЛЬКО активная из 3 реконструкций (`Suspense key=url`, кэш `useLoader`) → на входе один запрос ~4.5МБ вместо всех трёх ~13.7МБ и вместо прежних ~16МБ GLB; (2) `points` = один draw-call, без теней/PBR/Environment — легче прежней сцены и на GPU; (3) frameloop гейтится видимостью секции (never↔always); (4) канвас маунтится отложенно (rAF). В `Landing.jsx` (Engine): lazy-импорт переключён на `EngineCloudImpl`, поднят `recon`-стейт + добавлен доступный switcher `01/02/03` в копи-панель (`.kb-l-recon*` в `landing.css`) — остальные облака подгружаются по клику; на reduced-motion сцена и switcher не рендерятся (как и раньше). Удалён мёртвый код `MaterialShowcaseHeroImpl.jsx` + `ktx2gltf.js` (ни на что больше не ссылаются). `npm run build` зелёный: чанк `EngineCloudImpl` 2.97КБ (three остаётся в общем vendor-three, lazy). NB: GLB `public/models/{site_crane,block_sand_rock,stone_gravel,pile_of_gravel,cubes,standard_mirror...}.glb` (~37МБ суммарно) больше не используются ни в одном файле src и не в PWA-precache — можно удалить из репозитория (на загрузку страницы не влияют, но раздувают деплой).
- [2026-08-06] ЛЕНДИНГ, переделка под макет `Hero Video.dc.html` — ЭТАП 1/3 (скролл + хиро, «самое рискованное»). (0) УБРАН ПЛАВНЫЙ СКРОЛЛ: удалён `components/SmoothScroll.jsx` + импорт/`<SmoothScroll/>` в `App.jsx`, `lenis` из `frontend/package.json`, `html{scroll-behavior:smooth}` из `styles.css:45`, в `Landing.jsx` `scrollIntoView` smooth→auto — скролл нативный, без инерции (проверено: `data-lenis`/`.lenis`/`#smooth-*` не осталось). (1) ТЁМНАЯ ПАЛИТРА всей страницы через переменные `.kb-landing`: `--bg #0E140A` (= низ видео, одно значение), `--bg-2 #121A0C` (Движок), `--foot #0A0F07`, `--text #F1EDE1`, `--muted rgba(241,237,225,.68)`, `--faint .5`, `--gold #e7b552`, `--gold-deep #c98a24`, `--green #1E3D12`, границы/dash на white-alpha. (2) ХИРО = fullscreen `<video src=/landing/video.mp4 poster=/landing/hero.webp autoPlay loop muted playsInline preload=auto>` (three в хиро больше нет — только секция «Движок»); цветокор `saturate(.84) contrast(1.06) brightness(.97) sepia(.17) hue-rotate(-8deg)`. Заголовок → «Volumetric» (кремовый BlurText) + «Gottland» (золотой курсив, shimmer). Grid `auto minmax(0,1fr) auto` (pad·центр·низ), ВЕСЬ контент (заголовок/саб/кнопки/чипы) в центр.колонке `container-type:size`; типографика `min(8cqi,15cqh,96px)` и т.п. БЕЗ нижних clamp-полов, БЕЗ padding-bottom в cqh → не переполняется по вертикали. Чипы — под кнопками, тёмное стекло `rgba(16,22,11,.82)`, значения `#e7b552`. (3) РАСТВОРЕНИЕ низа видео в фон — 4 слоя (порядок важен, z:1→2, контент z:3): ① скрим-читаемость `opacity:var(--hero-scrim)`, ② нарастающий `backdrop-blur(18px)` h24% с маской, ③ ЧЕТЫРЕ овальных облака `#0E140A` `blur(30–42px)` на разной высоте (bottom −4%…4%, ширины 58–76% — разбивают горизонтальный фронт), ④ финальный градиент h11% до чистого `#0E140A`. Золото стыка — СИММЕТРИЧНЫЙ овал по центру `.kb-l-hero__glow` + зеркальное сверху `#dash`; `#dash{margin-top:-1px}`. Атмосфера-звёзды/пыль и курсорный параллакс из хиро убраны (конфликтовали со слоёным растворением; `useMemo` больше не нужен). Проверено headless-Chrome (`--force-prefers-reduced-motion`, финальные состояния) 1440×900 / 390×844 / 640×480 — заголовок/саб/2 кнопки/3 чипа влезают без клиппинга и оверфлоу; `npm run build` зелёный (Landing 24.5КБ, three в отдельном lazy vendor-three, не в главном бандле). ДАЛЬШЕ: этап 2 — бенто (тёмные карточки/числа), этап 3 — стекло (`.kb-l-glass` тёмное, орбы, хедер, флора). NB: макет-файл `Hero Video.dc.html` физически отсутствовал на диске — хиро собран строго по числовому ТЗ + существующие ассеты `public/landing/*`, `public/decor/flora-*`.
- [2026-08-11] ИСТОРИЯ: множественный выбор замеров + массовые действия (удалить / перезапустить с выбором ссылки TEST-PROD). ФРОНТ `frontend/src/pages/History.jsx`: режим выбора включается кнопкой «Выбрать» в шапке, двойным кликом по карточке, Ctrl/⌘/Shift+кликом и ДОЛГИМ ТАПОМ на тач-устройствах (500 мс, `touchmove` отменяет — не мешает скроллу; после лонг-тапа гасится один синтетический `click` через `skipClick`, иначе он тут же снимал выделение). Shift+клик тянет диапазон по видимому порядку (`view.flatIds` — плоский список id в том же порядке, что карточки на экране, считается в том же `useMemo`, что и группы). Панель массовых действий `BulkBar` — портал в `body` (страница скроллится внутри `.content`, панель должна быть fixed поверх боковой флоры), внутри: счётчик/прогресс, «Выбрать все», переключатель `.mode-toggle` TEST/PROD (тот же, что на странице анализа) и кнопки «Перезапустить»/«Удалить»; Esc выходит из режима. Массовые запросы гоняются пулом `mapPool(ids, 3, …)` — не «все разом» (десяток параллельных DELETE с чисткой Storage кладёт бэк) и не строго последовательно (на 30 замерах это минуты). Выбор чистится от исчезнувших id при каждом обновлении `items`; после перезапуска карточка оптимистично уходит в `pending`, дальше её дотягивает 10-секундный поллинг. `frontend/src/api.js`: `rerunAnalysis(id, {isProd, cube})` → `POST /api/analyses/{id}/rerun`. СТИЛИ `frontend/src/styles.css`: `.kh-check`, `.kh-card.is-selected`, `.kh-bulk*`, `.btn.is-active`, `body.kh-bulk-open .content{padding-bottom}` (панель не накрывает последнюю карточку) + мобильные правила. БЭК `backend/app/routers/analyses.py`: новый `POST /analyses/{analysis_id}/rerun` (тело `{is_prod, cube}`) — права как у delete (свой замер или суперадмин), затирает `result`/`completed_at`, ставит `pending` и в фоне зовёт `_rerun_and_save`: фото ПОВТОРНО НЕ ЗАГРУЖАЮТСЯ, а тянутся обратно из Storage по `colmap_photos.storage_path` (фолбэк — `public_url` через httpx), порядок берётся из `analyses.photo_urls`, `photo_ids` держится параллельным фото (иначе `meta.photo_count` врёт), дальше — существующий `_call_n8n_and_save` на выбранный вебхук. Парсинг блока куба вынесен в `_cube_block()` и переиспользован в `create_analysis`. Профиль для n8n берётся у ВЛАДЕЛЬЦА замера, а не у нажавшего (админ перезапускает чужие — письмо должно уйти владельцу). Перезапуск замера в статусе `pending` намеренно РАЗРЕШЁН: фоновые задачи умирают вместе с процессом uvicorn, и это единственный способ раскачать зависшую запись. ОГРАНИЧЕНИЕ: EXIF и параметры калибровочного куба в БД не хранятся — при перезапуске exif уходит пустым, а куб берётся из тела запроса (фронт пока не шлёт → стандартный 4×17.5 мм). `npm run build` и `py_compile` зелёные; вживую (с реальным n8n) не проверялось.
- [2026-08-13] ТЕМЫ: готическая `gtc` («свага») УДАЛЕНА ПОЛНОСТЬЮ — остались только `light` и `dark`. `theme/ThemeProvider.jsx`: `VALID = ['light','dark']`, из контекста убраны `isGtc`/`flipping`/`intro`/`skipIntro` и вся логика «разлома» с таймерами; `MIGRATE` теперь `{normal:'light', swag:'dark', gtc:'dark'}` — у кого в localStorage (`kh-theme`) лежала готика, тот при первом заходе получает тёмную (значение сразу перезаписывается эффектом сохранения, залипшего `gtc` на `<html data-theme>` не остаётся). `App.jsx`: убраны `<SwagAtmosphere/>`/`<IntroVeil/>`/`<Fracture/>`, `useTheme` и класс `.az-flip` — `.app-shell` теперь статичен. `components/swag/ThemeToggle.jsx` → `components/ThemeToggle.jsx` (2 позиции ☀/🌙, бегунок `translateX(index*100%)` работает без правок), импорт в `Layout.jsx` обновлён. `swag.css` → `theme-dark.css`: выброшены ~330 строк `[data-theme="gtc"]`, все `@keyframes` атмосферы/интро/разлома (fog/grain/glitch/crack/wipe/shake/shard/kenburns/breathe/ember/bats/introText/ringPulse/metalSheen/btnSweep/rayDrift), стили `.swag-atmos`/`.sa-*`/`.swag-intro`/`.si-*`/`.swag-fracture`/`.sf-*`/`.ts-gtc` и `@import` готических Google-шрифтов (UnifrakturMaguntia/Cook, Grenze Gotisch, Spectral — больше нигде не использовались → минус один внешний запрос шрифтов); осталась только тёмная палитра, перекраска светлых поверхностей, `.theme-switch` и тёмный `.tabbar`, импорт в `main.jsx` обновлён. Удалены файлы: `src/swag.css`, `src/components/swag/{SwagAtmosphere,IntroVeil,Fracture,ThemeToggle}.jsx`, `public/swag/*.png` (6 файлов, 3.0 МБ — не попадают ни в дист, ни в PWA-precache). Мелочи: в `Analyze.jsx` убран `useTheme`/`isGtc` (лейбл кнопки всегда «Анализируем...»), в `report-panel.css` — правило `[data-theme="gtc"] .rp-handle`, комментарии в `styles.css`/`report-panel.css` про «light/dark/gtc» поправлены. `npm run build` зелёный (70 модулей, 6.0s; главный CSS 54.3 КБ, в дисте `gtc` не встречается, `dist/swag/` не создаётся). Вживую в браузере не кликал — проверка только сборкой и grep'ом.

- [2026-08-15] ФИКС: в бакет уходил ИСПОРЧЕННЫЙ кадр — виноват клиент. `frontend/src/pages/Analyze.jsx` резал каждое фото canvas'ом до 1600 px по длинной стороне и пересохранял в JPEG q0.85 (`compressImage`): iPhone 12 3024×4032 → 1200×1600, EXIF срезался ЦЕЛИКОМ. Расчётный сервер берёт фокусное из EXIF — без него оно оптимизируется свободно и разъезжается втрое (391..1238 px по кадрам одной съёмки), плюс калибровочный куб на 1200 px занимал 5-7 px вместо 20+. Бэкенд и Storage были НИ ПРИ ЧЁМ: `analyses.py` и до этого лил `content` байт в байт, а миниатюра всегда была отдельным объектом (`{uuid}_thumb.jpg` → `colmap_photos.thumb_storage_path/thumb_url`) и оригинал не подменяла. НОВОЕ: логика вынесена в `frontend/src/prepareImage.js` — на сервер уходит сам `File` (JPEG/PNG/WebP), canvas остался ТОЛЬКО для превью 1024 px (сетка миниатюр + фото в PDF-отчёте, на сервер не уходит). HEIC/HEIF пайплайн не читает → конвертируем в JPEG, но в ПОЛНОМ разрешении без даунскейла (EXIF там теряется, но он уходит отдельным полем `exif_data`, его парсит exifr из оригинала ДО конвертации). Пустой `file.type` от части Android-пикеров определяется по расширению (иначе бэк отбивал «Не изображение»). GPS намеренно НЕ вырезаем — требование «побайтово оригинал»; в UI как и раньше значок геометки. `frontend/nginx.conf`: `client_max_body_size 100m` → `2g` (потолок бэкенда MAX_FILES×MAX_FILE_BYTES; на 100m пачка из 50 оригиналов ловила 413 примерно на 25-м кадре) + `proxy_request_buffering off` (иначе nginx пишет сотни МБ в client_temp до начала отдачи бэкенду). `backend/app/routers/analyses.py`: в payload к n8n добавлено поле `photo_urls` (ссылки на оригиналы — чтобы воркфлоу мог уйти с base64 без правки бэка) + лог размера base64-пачки, warning при >100 МБ. ПРОВЕРЕНО headless-Chrome (harness: prepareImage → IndexedDB → FormData → multipart, sha256 считался на серверной стороне): пришло 3024×4032, sha256 совпал с оригиналом бит в бит, APP1/Exif на месте, File пережил round-trip через IndexedDB; тот же прогон на СТАРОМ коде даёт 1200×1600, EXIF отсутствует, sha другой. РИСК, требует действия на инфре: `photos_b64` к n8n растёт ×5-6 (пачка 50 кадров ≈ 250 МБ JSON) — поднять `N8N_PAYLOAD_SIZE_MAX` и память контейнера бэкенда либо перевести воркфлоу на скачивание по `photo_urls`. Вживую с реального iPhone не гонял (нет доступа к деплою) — критерий приёмки с exiftool за пользователем.

- [2026-08-20] ДИАГНОСТИЧЕСКИЕ ИЗОБРАЖЕНИЯ АНАЛИЗА (карта высот + облако точек, сверху/сбоку) выведены на фронт. `backend/app/routers/analyses.py`: в `list_analyses` дописал в ЯВНЫЙ `.select(...)` четыре колонки `heatmap_top_url, heatmap_side_url, cloud_top_url, cloud_side_url` — без этого история отдавала бы их пустыми (у `GET /analyses/{id}` `select("*")`, там правок не нужно). НОВОЕ: `frontend/src/components/Diagnostics.jsx` + `frontend/src/diagnostics.css` — `pickDiagnostics(row)` (нормализует 4 колонки в `{views, items}`, возвращает `null`, если все NULL), `DiagnosticsBlock` (пара «карта ↔ облако» ШТОРКОЙ: обе картинки в одних осях/масштабе, верхний слой подрезан `clip-path: inset(0 calc(100% - var(--kd-x)) 0 0)`; переключатель Карта/Шторка/Облако рулит той же переменной 100%/0%/pos, ракурсы Сверху/Сбоку — вторым сегментом; ручка `role="slider"` + стрелки/Home/End; `touch-action: pan-y`, чтобы страница на телефоне всё ещё скроллилась), `DiagLightbox` (полный просмотр, Вписать/1:1/2×/4×, панорама скроллом контейнера + перетаскиванием, ←/→ листают тот же `<img>` БЕЗ сброса зума и позиции — карта и облако сняты одной камерой, и переключение на месте показывает, где облако пустое), `DiagThumb` (62×46 превью для карточки истории, сам держит свой лайтбокс). ЖЁСТКИЕ ИНВАРИАНТЫ (не оформление, а достоверность): везде только `object-fit: contain` — в нижние ~96 px PNG вшиты подписи и шкала, любой cover их срежет; `image-rendering: pixelated` ТОЛЬКО на увеличении (сглаживание размазывает пурпурную ячейку затяжки в фон), и «Вписать» ограничен `min(100%, 960px)`, чтобы браузер не растягивал кадр выше 1:1. Легенда цветов продублирована в UI по-русски (в самих PNG подписи латиницей): пурпур #EC00C8 — затянутая дыра, красный #F02D2D — обрыв края/недосъём, серый #48485A — пустая кайма. Подключено: `pages/Analyze.jsx` (state `diag` ← `pickDiagnostics(data)` на поллинге, блок под 3D-вьюером, сброс в `reset`/`runAnalysis`) и `pages/History.jsx` (`DiagThumb` в `.kh-card__aside`, полный блок в развороте под 3D). Деградация: все поля NULL → блока нет вовсе (ни скелетонов, ни пустых рамок), частичный набор рисует что есть и прячет шторку. Все 4 PNG `loading="lazy" decoding="async"`. ПРОВЕРЕНО headless-Chrome на сгенерированных мок-PNG 960×720: шторка/режимы/ракурсы, лайтбокс fit и 4× (одиночная пурпурная ячейка видна как чёткий квадрат), тёмная тема, узкая раскладка, NULL-случай; временный харнесс удалён. НЕ СДЕЛАНО (нужна отдельная задача): числа к картинкам — сколько ячеек достроено, какой % объёма это дало, доля обрыва периметра — лежат в `dust3r_results`, на фронт не приезжают; нужен join на бэкенде либо чтобы n8n клал их в `analyses.result`. Выдумывать/парсить из текста намеренно не стал.

- [2026-08-23] В n8n уходят байты ТОЛЬКО 2 лучших кадров + отбор переехал на сайт. Было: `_call_n8n_and_save` слал `photos_b64` со ВСЕЙ пачкой (~250 МБ JSON на 50 оригиналов и столько же в RAM бэкенда), а Code-нода воркфлоу сама парсила EXIF, скорила кадры и брала топ-2 для LLaVA — то есть n8n принимал и декодировал всю пачку ради двух кадров и перегружался. Стало (`backend/app/routers/analyses.py`): `photos_b64` ОСТАЛСЯ, формат прежний (data-URL) — нода «b64 → items» и «EXIF → parse» не правятся вообще — но в нём ровно 2 кадра. Остальные едут метаданными: `photos[]` (`{index, id, url, thumb_url, filename, exif, quality_score}`, `id` — строка `colmap_photos`, `url` — публичная ссылка на ОРИГИНАЛ), `best_photos[]` (те же два, с index/id/EXIF/оценкой), `meta.best_photo_indexes`/`best_photo_ids`, плюс прежние параллельные массивы `exif[]`/`photo_urls[]` по всей пачке. ЛОВУШКА, из-за неё правится нода-скоринг: `exif[]` нумерует всю пачку, `photos_b64[]` — только отобранные, поэтому `photos_b64[0]` это НЕ `photo_index 0`; брать `exifParsed[k]` по позиции в `photos_b64` нельзя — модель получит EXIF чужого кадра. Сопоставление через `meta.best_photo_indexes[k]`. Скоринг перенесён в бэкенд один в один (`_photo_quality_score`: ISO → `max(0, 1 - ISO/3200)`, BrightnessValue → 3-7 EV = 1.0, 1-10 EV = 0.6, остальное 0.2, поровну по 0.5 веса; нет EXIF → 0.5), синонимы тегов (`ISOSpeedRatings`/`PhotographicSensitivity`/строковые значения) учтены — фронтовый exifr пишет `ISO`/`BrightnessValue`, а не `iso`/`brightness`, как ожидала нода (она читала уже нормализованный выход «EXIF → parse»). ОТЛИЧИЕ ОТ ОРИГИНАЛА: в ноде ветка 0.2 была мёртвой (`brightness >= 1 || brightness <= 10` истинно всегда), здесь она живая — пересвет/темнота реально штрафуются. Отбор живёт в ОДНОМ месте — `_best_photo_indexes` (по позициям, тай-брейк по индексу → на пачке без EXIF выбор детерминирован), и вызывается ДО цикла загрузки: иначе ради двух кадров пришлось бы держать в памяти байты всех 50 (~150 МБ на каждый параллельный запрос). `_rerun_and_save` скачивает из Storage только 2 лучших вместо всей пачки; не скачалось — повтор не валится, у n8n остаются ссылки (фатально только если фото нет вовсе). EXIF теперь ХРАНИТСЯ в БД — `supabase/migration_photo_exif.sql` (`colmap_photos.exif JSONB`, `ADD COLUMN IF NOT EXISTS`), перед вставкой прогоняется через `_sanitize_exif` (вырезает нулевой байт — jsonb его не принимает, режет UserComment/MakerNote до 512 символов, гасит NaN/Infinity, ограничивает вложенность); благодаря этому rerun уходит с настоящим EXIF, раньше он слал `[None]*N` и «лучшими» становились просто первые два кадра. Бэкенд переживает деплой РАНЬШЕ миграции: `_insert_photo_row`/`_fetch_photo_rows` ловят ошибку ИМЕННО про колонку `exif` (по тексту), один раз гасят флаг `_photos_exif_column` и дальше работают без EXIF с warning в лог; любая другая ошибка падает честно, чтобы не потерять EXIF молча. Лог payload теперь в МБ, warning при >32 МБ (два 20-мегабайтных оригинала дают ~53 МБ — потолок `N8N_PAYLOAD_SIZE_MAX` рядом). `n8n/select-best-photos.js` — замена ноды-скоринга: отбор не считает, берёт `photos_b64` + `best_photos`, делает `prepareBinaryData` и отдаёт `_exif` по НАСТОЯЩЕМУ `photo_index`, плюс `photo_id`/`photo_url`/`quality_score`; есть фолбэк на старое поведение, если `best_photos` не приехал. Фронт НЕ трогал — `exif_data` он слал и раньше. ПРОВЕРЕНО локально: реальные `_call_n8n_and_save`/`_rerun_and_save` прогнаны на стабах fastapi/pydantic/httpx/supabase (перехват тела запроса) — в `photos_b64` ровно 2 строки в порядке съёмки, `photos_b64[k]` совпадает с `best_photos[k]`, байты НЕ утекают в `photos`/`best_photos` (иначе те же кадры уехали бы дважды), `exif[best_photos[k].index]` совпадает с EXIF кадра, тело 14 КБ + байты; rerun: 4 кадра → 2 скачаны из Storage (проверено по содержимому), EXIF не пустой, пустой Storage → замер всё равно уходит, нет фото вовсе → status error; отбор на 8 наборах EXIF (ISO50/BV5.1 → 0.9922, ISO3200/BV9.5 → 0.3, только GPS и без EXIF → 0.5), пачка без EXIF → [0,1], один кадр → [0]; санитайзер (NUL вырезан, 512 символов, NaN → None, список 200 → 64). Нода прогнана в node с моками `$input`/`$()`/`this.helpers`: 2 item'а, EXIF взят по photo_index 2 и 3 (ISO 100 и 32), а не по позициям 0-1 (ISO 2000/800) — то есть ловушка с индексами закрыта. НЕ ПРОВЕРЕНО вживую (нет доступа к деплою и n8n): реальный вебхук — за пользователем. ТРЕБУЕТСЯ ДЕЙСТВИЕ: накатить `supabase/migration_photo_exif.sql` и заменить ноду-скоринг на `n8n/select-best-photos.js`.
- [2026-08-29] Мобильный интернет: перестали выкидывать из аккаунта. Корень был в `AuthContext.jsx` — `onAuthStateChange((_, session) => setUser(session?.user ?? null))` плюс единственный `getSession()` без обработки ошибки: у истёкшего access-токена getSession лезет в сеть за refresh, на плохой связи возвращает `{session: null, error}`, мы ставили `user = null`, и `PrivateRoute` уводил на `/login` — при том, что refresh-токен живой (gotrue при сетевой ошибке сессию не стирает). Теперь пользователь обнуляется ТОЛЬКО по явному `SIGNED_OUT`/`USER_DELETED` либо когда сессии реально нет в хранилище; сетевой сбой → режим `degraded` (показываем последнего известного пользователя из кэша `kb-last-user`, в нём только id/email, без токенов) и молчаливая пере-проверка по `online` / `visibilitychange` / раз в минуту. Стартовый спиннер `PrivateRoute` ограничен `INITIAL_WAIT_MS=5000`: у getSession внутри свои таймаут и ретраи (суммарно до минуты), и всё это время экран был пустой — теперь пускаем по кэшу, а вердикт догоняет.
- [2026-08-29] `frontend/src/supabase.js`: клиент больше не дефолтный. (1) `global.fetch` — своя обёртка с таймаутом 20 c и 2 ретраями: у браузерного fetch таймаута НЕТ, и на LTE запрос к `/auth/v1/token` не падает, а ВИСИТ — вместе с ним висит всё, что ждёт токен, отсюда «сайт не работает вообще с мобильного». Ретраим только когда ответа не было; для refresh это безопасно в пределах reuse interval. (2) `storage` → новый `frontend/src/authStorage.js`: localStorage + зеркало в IndexedDB + память процесса, читаем сверху вниз, пишем во все. Дефолтный чистый localStorage на телефоне выселяется (iOS ITP чистит script-writable storage), и вместе с ним улетает refresh-токен. `storageKey` НЕ трогали — смена ключа = разлогин всех живых сессий.
- [2026-08-29] `frontend/src/api.js`: слой переписан под мобильную сеть. Таймаут 30 c на запрос; ретраи (сеть/таймаут/5xx) ТОЛЬКО для GET/HEAD — повтор POST `rerun` запустил бы пайплайн дважды; `401` → один прозрачный `refreshSession()` и повтор запроса любым методом (запрос до сервера дошёл, но выполнен не был → дублей нет). Добавлен класс `NetworkError` (`isNetwork: true`), по нему `queue.js` отличает обрыв связи от жёсткого отказа сервера и оставляет элемент в `queued`, а не жжёт `attempts`.
- [2026-08-29] Полоса загрузки фото стала показывать РЕАЛЬНУЮ отправку. Раньше единственная полоса (`compProg` в `Analyze.jsx`) считала локальную подготовку файлов по НОМЕРУ кадра и заполнялась до 100% ещё до начала аплоада — дальше шёл только спиннер, отсюда «полоса идёт сама по себе». Теперь: (1) подготовка считается по БАЙТАМ и только по уже обработанному файлу; (2) сама отправка идёт через XHR (`api.createAnalysis` → `uploadOnce`, `upload.onprogress`) — fetch про отправку не сообщает ничего в принципе; прогресс `{phase, loaded, total, pct}` прокинут через `flushItem(id, { onProgress })` в состояние `upProg`, на экране проценты + мегабайты, в кнопке «Отправка N%»; (3) фаза `server` (байты ушли, бэкенд раскладывает пачку в Storage) честно помечена как неопределённая — `.prog-bar.is-indeterminate` в `styles.css`, без выдуманных процентов. Таймаута у аплоада нет намеренно: 100 оригиналов на мобильной сети идут десятками минут.
- [2026-08-29] `Analyze.jsx`: починен залипший `busy` при обрыве сети. `flushItem` офлайн НЕ бросает — он оставляет замер в очереди и возвращает `null`, а `runAnalysis` отправлял этот `null` в `startPolling(null)`: опрос молча ничего не делал, страница навсегда оставалась со спиннером и бегущим таймером. Теперь пустой id обрабатывается как «не отправили» — тем же путём, что и `catch`.
- [2026-08-29] Отчёт перестал мигать на каждое нажатие. Причина: `title` из формы шёл пропом в `ReportPanel` → `useMemo` пересобирал `data` → новый `<RaschetDocument>` → `@react-pdf` перерисовывал PDF и менял `src` у iframe, а смена src = перезагрузка встроенного просмотрщика (белый кадр + прыжок к началу). Три правки: (1) `useIdleValue` — title/result/photos доезжают до документа через 600 мс тишины, то есть пересборка одна на слово, а не на символ; (2) `memo(ReportPanel)` + `useCallback` на `onOpen`/`onClose` в `Analyze.jsx` — иначе новая стрелка на каждый рендер сводила memo на нет; (3) `PdfPreview.jsx` переехал со штатного `<PDFViewer>` на `usePDF` + ДВОЙНУЮ БУФЕРИЗАЦИЮ: два iframe друг над другом, новый blob грузится в невидимый и слои меняются местами только по его `onLoad` (+ фолбэк 3 c, если onLoad не пришёл). Видимый кадр не трогаем — мигать нечему. Стили слоёв — `.rp__viewer-frame` / `.rp__viewer-note` / `.rp__viewer-badge` в `report-panel.css`.
- [2026-08-29] Новый `docs/AUTH_SESSION.md`: что осталось сделать НА СЕРВЕРЕ для «не выходить 30 дней». Supabase здесь self-hosted (`supabase.gottland.ru`), поэтому настройки не в дашборде, а в окружении контейнера `auth`: `GOTRUE_SESSIONS_TIMEBOX=720h` и `GOTRUE_SESSIONS_INACTIVITY_TIMEOUT=720h` (Go-длительности; переменную ПРОПУСТИТЬ = «никогда», ноль не проходит валидацию) — если там сейчас стоит что-то короткое, это и есть «выкидывает быстро», кодом не лечится; `GOTRUE_SECURITY_REFRESH_TOKEN_REUSE_INTERVAL=30` при включённой ротации — иначе потерянный на мобиле ответ на refresh оставляет клиента с мёртвым токеном навсегда; `GOTRUE_JWT_EXP` — срок access-токена, к 30 дням отношения не имеет (у бэкенда нет списка отзыва, поднимать осторожно).

- [2026-09-05] АУДИТ UI обеих поверхностей (лендинг + приложение), без правок. Ключевое: (1) тем в проекте ДВЕ (`light`/`dark`), `gtc` удалена 2026-08-13 — бриф «три темы» неисполним как есть; (2) ЛЕНДИНГ — недоделанная тёмная миграция: `PROGRESS.md:133` (2026-08-06) сделал этап 1/3 (палитра `--bg #0E140A` + видео-хиро), а этапы 2 (тёмное бенто) и 3 (тёмное стекло) не сделаны, поэтому `.kb-l-glass` остался белым (`rgba(255,255,255,.56→.30)`) и ВЕСЬ текст на нём кремовый/тёмно-зелёный → контраст ≈1.3:1 (бенто-метрики, «Конвейер», «Экспорт», панель Движка, 3 карточки Процесса, кино-панель); плюс `.kb-l-recon__tabs`, `.kb-l3d-loader` (тёмный текст на чёрном), `.kb-l-cine__veil`, светлые `::before`-разделители; (3) тёмная тема приложения покрыта на треть — `theme-dark.css` не переопределяет `--red-bg/--red/--amber/--shadow-sm/--shadow-lg`, отсюда бело-розовые `.btn-danger`/`.kh-card__error`/`.kh-del:hover`, белая `.kh-bulk__inner`, `#1A6B3C` на тёмном в `.kh-stat__value.is-ok` (≈1.9:1); (4) focus-ring `var(--green)` в dark даёт 2.3:1 при норме 3:1; (5) `History.jsx:1083` кладёт ЛЮБУЮ ошибку в `setError`, а рендер показывает красный блок ВМЕСТО списка → офлайн стирает журнал (при том что `api.js:30` уже отдаёт `NetworkError.isNetwork`); (6) тач-таргеты ниже 44px (`.thumb-rm` 18px, `.theme-switch__opt` 26px, `.icon-btn` 32px, `.kh-chevron` 30px); (7) вес: `public/models/*.glb` 38 МБ и `public/basis/*` 576 КБ не имеют НИ ОДНОЙ ссылки в `src/`, `cheremsha.png` 1.13 МБ + флора 412 КБ идут в precache SW, шрифты грузятся из Google Fonts тремя `@import` (в офлайне PWA типографика падает на system-ui), `vendor-three` 1.12 МБ тянется на рабочий экран `/app` из-за безусловного `<CubesHero/>`.
- [2026-09-05] ПАСС A — единая типографическая шкала и сетка отступов на ОБЕИХ поверхностях. `frontend/src/styles.css` (+ `landing.css`, `pages/Profile.css`, `report-panel.css`, `diagnostics.css`, `pages/Analyze.jsx`, `components/Layout.jsx`):
  - **Шкала кегля (G6), 11 ступеней** `--fs-3xs 10 / --fs-2xs 11 / --fs-xs 12 / --fs-sm 13 / --fs-md 14 / --fs-lg 15 / --fs-xl 17 / --fs-2xl 20 / --fs-3xl 23 / --fs-4xl 26 / --fs-5xl 32`. Было 27 разных кеглей, половина отличалась на 0.5–1.5px (10.5/12.5/14.5/15.5 на лендинге, 9/18/19/21/22/24/28/30/34 в приложении) — глаз читает такую разницу не как иерархию, а как неряшливость. Реально сдвинулось 34 значения, все ≤2px. Дисплейные заголовки (`.hero h1`, `.kh-h1`, `.page-title`, `.kb-l-*__title`) остались на `clamp()`; `.timer-clock` 48/36px переведён на `clamp(36px,9vw,48px)` (мобильный оверрайд убран).
  - **Сетка отступов (G7)**: шаг 4px (`--sp-N == N×4px`, от `--sp-1` 4px до `--sp-22` 88px) + ТРИ явных полушага `--sp-1-5` 6px / `--sp-2-5` 10px / `--sp-3-5` 14px — это зазоры ВНУТРИ компонента (чип, кнопка, строка меты), где 4px мало, а 8px разваливает группу. Всё, что было мимо сетки (9/11/13/17/22/26/30/34/38/46/54/70/90), сведено к ближайшей ступени: 94 значения, сдвиг ≤2px.
  - `--control-pad` / `--control-font` / `--label-size` больше не несут своих чисел, а ссылаются на шкалы (`var(--sp-3) var(--sp-3-5)` / `var(--fs-md)` / `var(--fs-2xs)`) — иначе контролы уезжают от остальной типографики при правке шкалы.
  - **Побочно найдено и починено: гвардия «16px, чтобы iOS не зумил» не работала для трёх полей.** Она стояла в `@media` ДО базовых правил `.kh-search input` / `.kh-range input` / `.kh-userpick`, а медиазапрос НЕ повышает специфичность → базовые правила её перебивали, и тап в поиск Истории зумил страницу на айфоне. Гвардия вынесена в БЛОК В САМОМ КОНЦЕ `styles.css` («MOBILE INPUT ZOOM GUARD») — выше по файлу её поднимать нельзя. Там же удалён мёртвый дубль всего блока `.kh-userpick` из мобильного `@media`: он был перебит базовым правилом целиком, а внутри имел `background: var(--card-bg, #fff)` — переменной `--card-bg` в проекте нет (она зовётся `--bg-card`), так что фолбэк красил селект в БЕЛЫЙ в тёмной теме. 16px — функциональное значение, в шкалу не заведено намеренно.
  - **Побочно найдено и починено: глобальный `footer {}` протекал в подвал лендинга.** `footer { display:flex; justify-content:space-between; margin-top:40px; font-size:12px }` применялся и к `.kb-l-foot` (тот своего `display` не задаёт), раскладывая его две полосы в строку вместо стопки — та же ловушка, что с глобальным `header` (см. комментарий у `.kh-head` в `History.jsx`). Правило скоуплено в класс `.app-foot` / `.app-foot__row` / `.app-foot__link`, инлайн-стили подвала из `Layout.jsx` убраны.
  - `Layout.jsx`: копирование email было `<span onClick>` (мышью работает, с клавиатуры нет) → `<button type="button">`; `alert('Email скопирован')` заменён на состояние прямо в кнопке (2 c) — модалка ради этого перебивала фокус и на мобиле требовала лишнего тапа.
  - `Analyze.jsx`: лейбл «Режим анализа» жил своим инлайн-кеглем `12px/0.5px` вместо спека полей `--label-*` — снят, работает `.field label`; остальные инлайн-отступы карточки результата переведены на `--sp-*`.
  - Вес: `styles.css` минифицированный+gzip 9747 → 10008 B (**+261 B**) — `var(--sp-N)` длиннее `12px` в исходнике, но после gzip это шум. `npm run build` зелёный.
