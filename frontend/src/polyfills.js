/* ============================================================
   polyfills.js — Node-глобалы для браузера.
   ------------------------------------------------------------
   Нужны @react-pdf/renderer (Buffer, global) и части его зависимостей
   (process.env). Раньше global/process ставились инлайн-скриптом в
   index.html — вынесли сюда, чтобы CSP script-src 'self' работал
   без 'unsafe-inline'.

   ВАЖНО: этот файл должен быть ПЕРВЫМ импортом в main.jsx —
   ES-модули исполняются в порядке импортов, так что глобалы
   встанут до инициализации react/supabase и всего остального.
   Тяжёлые потребители (react-pdf) теперь вообще в lazy-чанках,
   т.е. грузятся сильно позже этого кода.
   ============================================================ */
import { Buffer } from 'buffer'

window.global = window
window.process = { env: {} }
window.Buffer = Buffer
